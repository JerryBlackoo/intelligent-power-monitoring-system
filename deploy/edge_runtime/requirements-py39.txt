# Python 3.9 dependencies for the Atlas edge runtime service.
# Install these on the development board instead of the full project requirements.

fastapi>=0.95,<0.112
uvicorn[standard]>=0.22,<0.30
pydantic>=1.10,<3
httpx>=0.23,<0.28
python-multipart>=0.0.6

# Camera capture uses cv2 only when EDGE_USE_CAMERA=1.
# On Atlas/aarch64, prefer the system package when available:
#   apt-get install -y python3-opencv v4l-utils
